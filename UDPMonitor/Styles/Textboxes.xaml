<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- ========================================================= -->
    <!-- Shared values                                              -->
    <!-- ========================================================= -->
    <Thickness x:Key="TextBox.Padding">10,6</Thickness>
    <Thickness x:Key="TextBox.PaddingSmall">8,4</Thickness>

    <sys:Double x:Key="TextBox.MinHeight">30</sys:Double>
    <sys:Double x:Key="TextBox.MinHeightSmall">26</sys:Double>

    <!-- ========================================================= -->
    <!-- TextBox base                                               -->
    <!-- ========================================================= -->
    <Style x:Key="TextBox.Base" TargetType="{x:Type TextBox}">
        <Setter Property="FontFamily" Value="{StaticResource Font.App}" />
        <Setter Property="FontSize" Value="{StaticResource FontSize.Body}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
        <Setter Property="Background" Value="{StaticResource Brush.Separator}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="{StaticResource TextBox.Padding}" />
        <Setter Property="MinHeight" Value="{StaticResource TextBox.MinHeight}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="CaretBrush" Value="{StaticResource Brush.Caret}" />
        <Setter Property="SelectionBrush" Value="{StaticResource Brush.TextSelection}" />
        <Setter Property="SelectionOpacity" Value="1" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Grid>
                        <!-- Outer border -->
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" />

                        <!-- Focus ring (shown only on focus) -->
                        <Border x:Name="FocusRing"
                                BorderBrush="{StaticResource Brush.FocusRing}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Opacity="0"
                                Margin="0" />

                        <!-- Content -->
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="1"
                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Grid>

                    <ControlTemplate.Triggers>

                        <!-- Hover -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Brush.TextSecondary}" />
                        </Trigger>

                        <!-- Focus -->
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Brush.Accent}" />
                            <Setter TargetName="FocusRing" Property="Opacity" Value="0.9" />
                        </Trigger>

                        <!-- Disabled -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Brush.TextDisabled}" />
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Brush.DisabledSurface}" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Brush.DisabledBorder}" />
                        </Trigger>

                        <!-- ReadOnly -->
                        <Trigger Property="IsReadOnly" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Brush.Surface}" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Brush.Border}" />
                            <Setter Property="Foreground" Value="{StaticResource Brush.TextSecondary}" />
                        </Trigger>

                        <!-- Validation error (if you use WPF validation) -->
                        <Trigger Property="Validation.HasError" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Brush.Danger}" />
                            <Setter TargetName="FocusRing" Property="BorderBrush" Value="{StaticResource Brush.Danger}" />
                            <Setter TargetName="FocusRing" Property="Opacity" Value="0.9" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========================================================= -->
    <!-- Variants                                                   -->
    <!-- ========================================================= -->

    <!-- Small -->
    <Style x:Key="TextBox.Small" TargetType="{x:Type TextBox}" BasedOn="{StaticResource TextBox.Base}">
        <Setter Property="Padding" Value="{StaticResource TextBox.PaddingSmall}" />
        <Setter Property="MinHeight" Value="{StaticResource TextBox.MinHeightSmall}" />
    </Style>

    <!-- Monospace (IPs, ports, payloads, timestamps) -->
    <Style x:Key="TextBox.Mono" TargetType="{x:Type TextBox}" BasedOn="{StaticResource TextBox.Base}">
        <Setter Property="FontFamily" Value="{StaticResource Font.Mono}" />
    </Style>

    <!-- Small + Monospace -->
    <Style x:Key="TextBox.MonoSmall" TargetType="{x:Type TextBox}" BasedOn="{StaticResource TextBox.Small}">
        <Setter Property="FontFamily" Value="{StaticResource Font.Mono}" />
    </Style>

    <!-- Error (manual state, if you want to set it yourself) -->
    <Style x:Key="TextBox.Error" TargetType="{x:Type TextBox}" BasedOn="{StaticResource TextBox.Base}">
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Danger}" />
    </Style>

    <!-- ========================================================= -->
    <!-- PasswordBox (matching look)                                -->
    <!-- ========================================================= -->
    <Style x:Key="PasswordBox.Base" TargetType="{x:Type PasswordBox}">
        <Setter Property="FontFamily" Value="{StaticResource Font.App}" />
        <Setter Property="FontSize" Value="{StaticResource FontSize.Body}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
        <Setter Property="Background" Value="{StaticResource Brush.SurfaceRaised}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="{StaticResource TextBox.Padding}" />
        <Setter Property="MinHeight" Value="{StaticResource TextBox.MinHeight}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type PasswordBox}">
                    <Grid>
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource CornerRadius.S}" />
                        <Border x:Name="FocusRing"
                                BorderBrush="{StaticResource Brush.FocusRing}"
                                BorderThickness="1"
                                CornerRadius="{StaticResource CornerRadius.S}"
                                Opacity="0" />
                        <ScrollViewer x:Name="PART_ContentHost" Margin="1"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Brush.TextSecondary}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Brush.Accent}" />
                            <Setter TargetName="FocusRing" Property="Opacity" Value="0.9" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Brush.TextDisabled}" />
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Brush.DisabledSurface}" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Brush.DisabledBorder}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========================================================= -->
    <!-- ComboBox (base look, no fancy animations)                  -->
    <!-- ========================================================= -->
    <Style x:Key="ComboBox.Base" TargetType="{x:Type ComboBox}">
        <Setter Property="FontFamily" Value="{StaticResource Font.App}" />
        <Setter Property="FontSize" Value="{StaticResource FontSize.Body}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
        <Setter Property="Background" Value="{StaticResource Brush.SurfaceRaised}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="{StaticResource TextBox.PaddingSmall}" />
        <Setter Property="MinHeight" Value="{StaticResource TextBox.MinHeight}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Grid>
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource CornerRadius.S}" />
                        <Border x:Name="FocusRing"
                                BorderBrush="{StaticResource Brush.FocusRing}"
                                BorderThickness="1"
                                CornerRadius="{StaticResource CornerRadius.S}"
                                Opacity="0" />

                        <Grid Margin="6,0,28,0">
                            <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              RecognizesAccessKey="True"/>
                        </Grid>

                        <!-- Drop arrow -->
                        <Path Data="M 0 0 L 4 4 L 8 0 Z"
                              Fill="{StaticResource Brush.TextSecondary}"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Center"
                              Margin="0,0,10,0" />

                        <Popup x:Name="PART_Popup"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               Placement="Bottom"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Fade">
                            <Border Background="{StaticResource Brush.Surface}"
                                    BorderBrush="{StaticResource Brush.Border}"
                                    BorderThickness="1"
                                    CornerRadius="{StaticResource CornerRadius.S}"
                                    SnapsToDevicePixels="True">
                                <ScrollViewer Margin="0" SnapsToDevicePixels="True">
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Brush.TextSecondary}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Brush.Accent}" />
                            <Setter TargetName="FocusRing" Property="Opacity" Value="0.9" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Brush.TextDisabled}" />
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Brush.DisabledSurface}" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Brush.DisabledBorder}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Item style for ComboBox dropdown -->
    <Style TargetType="{x:Type ComboBoxItem}">
        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
        <Setter Property="Padding" Value="10,6" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Brush.SurfaceAlt}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Brush.SelectionBackground}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Brush.TextDisabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="TextBox.Editor" TargetType="{x:Type TextBox}" BasedOn="{StaticResource TextBox.Base}">
        <!-- editor feel -->
        <Setter Property="FontFamily" Value="{StaticResource Font.Mono}" />
        <Setter Property="AcceptsReturn" Value="True" />
        <Setter Property="AcceptsTab" Value="True" />
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="TextWrapping" Value="NoWrap" />
        <Setter Property="VerticalContentAlignment" Value="Top" />
        <Setter Property="Padding" Value="12,10" />
        <Setter Property="MinHeight" Value="220" />
    </Style>

</ResourceDictionary>
