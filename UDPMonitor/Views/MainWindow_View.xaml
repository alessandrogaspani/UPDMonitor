<Window x:Class="UDPMonitor.Views.MainWindow_View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        xmlns:local="clr-namespace:UDPMonitor"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Title="{Binding Title}" Height="350" Width="525" >
    <DockPanel>

        <!-- Menu top -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Help">
                <MenuItem Header="_About"
                          Command="{Binding AboutCommand}" />
            </MenuItem>
        </Menu>

        <!-- Contenuto -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ContentControl Grid.Column="0" prism:RegionManager.RegionName="{x:Static local:RegionNames.InboundRegion}" />
            <ContentControl Grid.Column="1" prism:RegionManager.RegionName="{x:Static local:RegionNames.OutboundRegion}" />
        </Grid>

    </DockPanel>
</Window>
